{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AireAcondicionado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pin</th>
                <td>{{ entity.pin }}</td>
            </tr>
            <tr>
                <th>Posicionx</th>
                <td>{{ entity.posicionX }}</td>
            </tr>
            <tr>
                <th>Posiciony</th>
                <td>{{ entity.posicionY }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ac') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ac_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
